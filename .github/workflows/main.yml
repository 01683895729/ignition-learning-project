name: Test Pipeline
on:
  push:
    branches: [main]
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Print message
        run: echo "Hello Roman, CI/CD is working!"
